{
  "name": "luct-reporting-app", 
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix ./frontend install && npm --prefix ./frontend run build",
    "start": "npm --prefix ./backend start"
  }
}
